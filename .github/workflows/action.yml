name: project-deployer
on: push

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: cloning repo
        uses: actions/checkout@v4

      - name: run compose
        run: |
          pwd
      # Этот шаг запускает Docker Compose в фоновом режиме.
      #trying forever
      #dafg